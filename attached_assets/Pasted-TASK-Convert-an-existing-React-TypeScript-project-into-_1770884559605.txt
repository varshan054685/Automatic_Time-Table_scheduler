TASK: Convert an existing React + TypeScript project into
React + JavaScript (JSX only) and fix sidebar navigation and pages.

TECH REQUIREMENTS:
- React with JavaScript only (no TypeScript)
- Replace all .ts and .tsx files with .js and .jsx
- Remove all TypeScript types, interfaces, and generics
- Use React Router for navigation
- Tailwind CSS for styling

SIDEBAR STRUCTURE (FIXED):

Dashboard
Departments
Classrooms
Subjects
Faculty
Sections
Time Slots
Timetable

ROUTING REQUIREMENTS:
Each sidebar item must open its OWN page:

/dashboard        → Dashboard
/departments      → Departments Management
/classrooms       → Classrooms Management
/subjects         → Subjects Management
/faculty          → Faculty Management
/sections         → Sections Management
/timeslots        → Time Slots Management
/timetable        → Timetable View

PAGE BEHAVIOR:
- Clicking "Subjects" should open ONLY "Subjects Management"
- Clicking "Sections" should open ONLY "Sections Management"
- Do NOT reuse the same component for multiple pages
- Each page must have its own component file

CRUD PAGE IMPLEMENTATION:
Departments, Subjects, Faculty, Sections, Classrooms, Time Slots
should ALL follow the same CRUD pattern:

- List view (table)
- Add modal or form
- Edit modal or form
- Delete action
- Back to Dashboard button

EXAMPLE PAGES:
- Subjects Management Page
- Sections Management Page

Each page must:
- Have a clear title
- Use reusable table and form components
- Follow the same UI structure as Departments/Faculty

PROJECT STRUCTURE (FRONTEND):

src/
 ├── pages/
 │    ├── Dashboard.jsx
 │    ├── Departments.jsx
 │    ├── Subjects.jsx
 │    ├── Faculty.jsx
 │    ├── Sections.jsx
 │    ├── Classrooms.jsx
 │    ├── TimeSlots.jsx
 │    └── Timetable.jsx
 ├── components/
 │    ├── Sidebar.jsx
 │    ├── Navbar.jsx
 │    ├── Table.jsx
 │    └── Form.jsx
 ├── App.jsx
 └── main.jsx

IMPORTANT FIXES:
- Fix sidebar links so they route correctly
- Remove duplicate page rendering issues
- Ensure navigation works correctly when clicking back to Dashboard
- Use JavaScript only (no TypeScript)

OUTPUT REQUIRED:
- Updated React project using only JavaScript
- Clean routing
- Correct sidebar behavior
- Working CRUD pages